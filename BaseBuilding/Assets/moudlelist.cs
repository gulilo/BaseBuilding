using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class moudlelist : MonoBehaviour {

	public GameObject gamey;

	// Use this for initialization
	void Start () {
		
	}

	// Update is called once per frame
	void Update()
	{
		
	}

	public void bla()
	{
		/*GetComponent<Text>().text = "";
		for (int i = 0; i < gamey.GetComponent<mainGame>().all.Length; i++)
		{
			if(gamey.GetComponent<mainGame>().all[i] != null)
				GetComponent<Text>().text += gamey.GetComponent<mainGame>().all[i].name;
		}*/
	}
}
